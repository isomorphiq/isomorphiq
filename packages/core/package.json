{
    "name": "@isomorphiq/core",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "main": "src/index.ts",
    "types": "src/index.ts",
    "exports": {
        ".": {
            "import": "./src/index.ts",
            "types": "./src/index.ts"
        }
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc --noEmit -p tsconfig.json",
        "lint": "echo \"Lint not configured yet for @isomorphiq/core\"",
        "test": "node --test --experimental-strip-types ../../scripts/workspace-test.ts"
    }
}
