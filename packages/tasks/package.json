{
    "name": "@isomorphiq/tasks",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "main": "src/index.ts",
    "types": "src/index.ts",
    "exports": {
        ".": {
            "import": "./src/index.ts",
            "types": "./src/index.ts"
        }
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc --noEmit -p tsconfig.json",
        "lint": "echo \"Lint not configured yet for @isomorphiq/tasks\"",
        "test": "echo \"Tests not configured yet for @isomorphiq/tasks\""
    },
    "dependencies": {
        "@isomorphiq/acp": "workspace:*",
        "@isomorphiq/core": "workspace:*",
        "@isomorphiq/integrations": "workspace:*",
        "@isomorphiq/persistence-level": "workspace:*",
        "@isomorphiq/realtime": "workspace:*",
        "@isomorphiq/search": "workspace:*",
        "@isomorphiq/user-profile": "workspace:*",
        "@tsimpl/core": "0.1.1",
        "@tsimpl/runtime": "0.1.1",
        "@tsimpl/validation": "0.1.1",
        "level": "^10.0.0",
        "zod": "^3.24.2"
    }
}
