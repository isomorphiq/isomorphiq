{
    "name": "@isomorphiq/cli",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "main": "src/index.ts",
    "types": "src/index.ts",
    "exports": {
        ".": {
            "import": "./src/index.ts",
            "types": "./src/index.ts"
        }
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc --noEmit -p tsconfig.json",
        "lint": "echo \"Lint not configured yet for @isomorphiq/cli\"",
        "test": "echo \"Tests not configured yet for @isomorphiq/cli\"",
        "start": "node --experimental-strip-types src/cli-client.ts"
    },
    "dependencies": {
        "@isomorphiq/auth": "workspace:*",
        "@isomorphiq/core": "workspace:*",
        "@isomorphiq/tasks": "workspace:*"
    }
}
