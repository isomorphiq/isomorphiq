import type { ReactNode } from "react";
export declare function SectionCard({ title, countLabel, children, }: {
    title: string;
    countLabel?: string;
    children: ReactNode;
}): import("react/jsx-runtime").JSX.Element;
